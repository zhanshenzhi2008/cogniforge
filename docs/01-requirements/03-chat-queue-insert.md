# Playground 排队与插入（一期）

## [变更记录]

| 日期 | 版本 | 变更摘要 | 负责人 |
|------|------|---------|--------|
| 2026-08-22 | v1.1 | 编辑改到发送框；行上「编辑中 ×」取消，无弹层 | orjrs |
| 2026-08-21 | v1.0 | 锁定：排队（Enter 入队、可编辑删除）+ 插入硬截断确认（方案 A） | orjrs |

## [变更] 排队编辑改到发送框（2026-08-22）

- **变更原因**：行内弹层改字不好用；取消按钮也不该出现在发送区
- **变更后**：点编辑 → 内容进发送框；回车保存回排队；该行显示「编辑中 ×」取消
- **~~点正文弹出编辑框~~**（2026-08-22）

## [变更] 立项锁定（2026-08-21）

- **变更原因**：流式中无法连问；改中间一问需要可控截断
- **决策**：
  1. 排队 / 插入含义已确认
  2. 插入后旧内容：**硬截断 + 确认**（不做 ‹ › 分支树；分叉新会话留二期）
  3. 流式中 **Enter = 加入排队**
  4. 排队条目：**可编辑、可删除、可调序**；「发送中」锁定

---

## 1. 一句话

- **排队**：AI 还在答时，下一条先进队，答完自动发；队里能改字、能删。
- **插入**：在某条消息后插一问，**删掉后面所有消息**（先确认），再发新问题。

---

## 2. 排队

| 规则 | 说明 |
|------|------|
| 入队 | 流式中 Enter，或点「加入排队」 |
| 上限 | 每会话最多 **5** 条 |
| 状态 | `queued`（可改删调序）/ `sending`（锁定） |
| 编辑 | 点铅笔把内容放进**发送框**改；回车/发送=保存回排队（不发出）；行上「编辑中 ×」取消（Esc 同）；发送框不出现取消按钮 |
| 删除 | `queued` 直接删，无二次确认 |
| 调序 | ↑↓（编辑中该行只留「编辑中 ×」，不调序） |
| 持久化 | 存在 `chat_conversations.message_queue`，刷新不丢 |
| 插入时 | **清空**本会话排队，避免截断后自动发旧队 |
| 自动发送 | 正在编辑的那条先不发出，保存或取消后再轮到它 |

流式结束后：取队首 → `sending` → 当作用户消息发出 → 成功后出队继续下一条。

---

## 3. 插入（方案 A）

1. 用户消息 hover →「在此后插入」
2. 出现插入草稿条
3. 确认：「将删除此条之后的 N 条消息，不可恢复」
4. 截断 `messages` → 追加新用户消息 → 清空 queue → 开始生成 → 存档

---

## 4. 数据

`message_queue` JSONB 元素：

```json
{ "id": "uuid", "content": "...", "images": [], "status": "queued", "sort": 0 }
```

API：`GET/PUT /api/v1/conversations/:id` 读写 `message_queue`；列表摘要可不带队列正文。

---

## 5. 一期不做

- ChatGPT 式 ‹ › 隐藏分支树
- 「分叉成新会话」（二期）
- Cmd+Enter 停止并立刻发送（可很快补）
- 跨会话全局队列
- PDF / Word 等非图片文件（**阶段十三**，等 RAG 完全没问题，见 `04-playground-files-rag.md`）
