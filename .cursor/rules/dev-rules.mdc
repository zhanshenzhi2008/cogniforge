---
description: cogniforge 项目专属规则（叠加在全局规则之上）
globs:
alwaysApply: true
---

# cogniforge 项目专属规则

> 本文件**叠加在全局规则** `~/.cursor/rules/global-coding.mdc` 之上。
> 全局规则（文档版本号、变更记录格式、Git 提交规范、安全规范）**已自动生效**，无需重复定义。

---

## 1. 项目文档目录

```
docs/
├── 01-requirements/      # 需求文档
├── 02-architecture/      # 架构设计
├── 03-api/              # API 文档
├── 04-database/         # 数据库设计
├── 05-frontend/         # 前端设计
├── 06-devops/           # DevOps（部署、CI/CD）
└── 99-dev-plan/         # 开发计划、已知问题
```

所有改动必须找到对应模块，不存在则新增。

## 2. 代码变更 → 文档映射

| 代码变更类型 | 必须更新的文档 |
|-------------|--------------|
| 修改 API 接口 | `docs/03-api/01-api-design.md` |
| 修改数据库表结构 | `docs/04-database/01-database-design.md` |
| 修改前端页面/组件 | `docs/05-frontend/01-frontend-design.md` |
| 修改架构设计 | `docs/02-architecture/01-technical-architecture.md` |
| 修改项目目录结构 | `docs/99-dev-plan/02-project-structure.md` |
| 新增功能/模块 | 最相关文档 + `docs/99-dev-plan/01-development-plan.md` |
| 修改部署/CI/CD | `docs/06-devops/01-deployment.md` |

AI 生成代码后必须主动提示需要更新的文档路径。

## 3. 涉及到的仓库

| 仓库 | 路径 | 文档主目录 |
|------|------|----------|
| `cogniforge` | `/opt/project/cogniforge` | 本仓库 `docs/` |
| `cogniforge-ai` | `/opt/project/cogniforge-ai` | 本仓库 `docs/` |
| `cogniforge-web` | `/opt/project/cogniforge-web` | 本仓库 `docs/` |

> 三个仓库**各自维护自己的 `docs/`**，跨仓库变更要在两个仓库的 `docs/99-dev-plan/01-development-plan.md` 中同步记录。

## 4. 服务器路径规范（cogniforge 三仓库专用）

| 用途 | 路径 |
|------|------|
| 应用代码 | `/opt/project/cogniforge(-ai|-web)/` |
| 公用数据库 | `/opt/databases/` |
| Traefik + Portainer | `/opt/docker/` |
| 部署 compose 文件 | 仓库根目录 `docker-compose.yml`（**已生产化，无 `traefik-docker/` 子目录**） |

## 5. Redis 键命名（强制）

公用 Redis **只用 db0**。多项目用**键前缀**隔离，禁止 `SELECT 1/2`（Cluster 只有 db0；`FLUSHDB` 也容易误伤别的项目）。

| 规则 | 说明 |
|------|------|
| 前缀 | 本项目所有键必须以 `cogniforge:` 开头 |
| 格式 | `cogniforge:{模块}:{名字}`，小写，冒号分层 |
| 禁止 | `cf:`、无前缀、把明文 API Key 写入 Redis |

已落地（Go / Python 共用，改名必须两边一起改）：

- `cogniforge:modelcfg:rev` — 配置版本，模型页增删改后 INCR
- `cogniforge:modelcfg:snapshot` — JSON；只存 `encrypted_key`，明文 Key 只留 Go 进程内存

新键先写进 `docs/04-database/01-database-design.md` §5.1，再写代码。

## 6. MySQL 数据库命名规范

### 6.1 字符集与排序规则

| 配置项 | 值 |
|--------|-----|
| 字符集（CHARSET） | `utf8mb4` |
| 排序规则（COLLATE） | `utf8mb4_0900_ai_ci` |

> `utf8mb4_0900_ai_ci`：MySQL 8.0+ 支持，**ai** = accent insensitive（变音符号不敏感），**ci** = case insensitive（大小写不敏感）。优先使用此排序规则。

### 6.2 索引命名规范

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| 普通索引 | `idx_{字段1}[_{字段2}[_{字段3}...]]` | `idx_user_id`、`idx_created_at_status` |
| 唯一索引 | `uk_{字段1}[_{字段2}...]` | `uk_user_email`、`uk_conv_msg_seq` |
| 外键索引 | `fk_{表名}_{ReferencedTable}` | `fk_message_conversation`、`fk_conv_user` |

### 6.3 字段缩写规则（索引名超 64 字符时使用）

字段名过长时按以下规则缩写，缩写必须**可读、可猜**：

| 完整字段 | 缩写 | 规则说明 |
|----------|------|----------|
| `project_id` | `proj_id` | 保留首尾辅音字母 |
| `conversation_id` | `conv_id` | 保留首尾辅音字母 |
| `message_id` | `msg_id` | 同上 |
| `sequence` | `seq` | 常用简写 |
| `created_at` | `crt_at` / `ct_at` | 保留关键词 |
| `updated_at` | `upd_at` / `ut_at` | 同上 |
| `provider_id` | `prov_id` | 保留首尾 |
| `model_id` | `mdl_id` | 同上 |
| `content` | `cnt` | 极常用简写 |
| `timestamp` | `ts` | 国际通用简写 |
| `identifier` | `id` | 本身就是缩写 |
| `is_deleted` | `del` | 布尔字段前缀 `is_` 去掉 |
| `status` | `st` | 短词不缩写 |

**缩写原则**：
- 缩写后总长度不超过 **64 字符**（MySQL 索引名上限）
- 优先保留字段的**核心语义**，去掉元音可大幅缩短
- 常见通用词（`id`、`at`、`ts`）不重复缩写
- 新缩写需在团队内达成共识，避免歧义

### 6.4 索引名长度处理流程

1. 原始字段拼接 → `idx_field1_field2_field3...`
2. 若 ≤ 64 字符，直接使用
3. 若 > 64 字符，按优先级裁剪字段或应用缩写：
   - 去掉 `id`、`at` 等通用后缀（可推知）
   - 字段按业务重要性排序，删掉最不重要的字段
   - 对长字段应用缩写规则
4. 最终仍超 64 字符时，在文档中说明原因并记录。

### 6.5 命名速查表（高频字段）

```
user_id      → u_id / uid        (uid 最常用)
project_id   → proj_id
conversation_id → conv_id / cnv_id
message_id   → msg_id
provider_id  → prov_id
model_id     → mdl_id
created_at   → crt_at / ct_at
updated_at   → upd_at / ut_at
is_deleted   → del_flag / del
status       → st
sequence     → seq
content      → cnt
timestamp    → ts
```
